import { Box } from '@mui/material';

const Navbar = () => {
    return(
        <Box bgcolor="lightsalmon" flex={1} p={2} sx={{ display: { xs: "none", md: "block" } }}>Navbar</Box>
    )
}

export default Navbar