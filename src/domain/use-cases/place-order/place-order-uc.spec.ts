import { placeOrderUC } from './place-order-uc';

describe('domain:use-cases:place-order', () => {
  //TODO: Implement this test when UC is ready
  test('It should return a string', () => {
    const result = placeOrderUC();
    expect(result).toBe('placeOrderUC');
  });
});
